% Части условия выделены подпунктами
Условие\ldots
\\\emph{(1)}
\\\emph{(2)}
\\\emph{(3)}
\\
Продолжение условия\ldots

% Решение задачи без ответа (на доказательство).
\solution
<Решение.>

% Времена в пределах одного решения должны быть согласованы
% (в разумных пределах).

% Геометрические обозначения с индексами должны быть разбиты пробелами,
% со штрихами~--- нет.
$A B_1 C = AB'C$.

% Решение задачи с ответом
\solution
\emph{Ответ:} <ответ>.
\par
<Решение.>

% Решение задачи с оценкой и примером.
\solution
\emph{Ответ:} <ответ>.
\par
\emph{Оценка.}
<Оценка.>
\par
\emph{Пример:} <пример>.

% Альтернативные решения
\solution
<Первое решение.>
\par
\emph{Другое решение.}
<Второе решение.>

% Решение из нескольких больших частей
\solution
<Лемма.>
\\\emph{Доказательство леммы.}
<Доказательство.>
\par
<Продолжение решения.>

% Картинка к решению
\ifsolution
\begin{figure}\centering
    \jeolmfigure[width=0.5\textwidth]{solution}
    \caption{к задаче \ref{<problem metapath>:solution}}
    \label{<problem metapath>:solution:fig}
\end{figure}%
\fi % \ifsolution

% Ссылка на картинку из решения
\solution
\label{<problem metapath>:solution}%
Рис.~\ref{<problem metapath>:solution:fig}.

% Дублирование задач: префикс задачи
% nospell begin
\def\ifsolutiondefined{%
    \csname ifsolution:<original metapath>\endcsname}%
\expandafter\providecommand
    \csname ifsolution:<original metapath>\endcsname
    {\iffalse}%
\def\definesolution{%
    \expandafter\gdef
    \csname ifsolution:<original metapath>\endcsname
    {\iftrue}}%
% nospell end

% Дублирование задач: маркер оригинала
\problem
% duplicated in [<duplicate metapath>]

% Дублирование задач: маркер копии
\problem
% duplicate of [<original metapath>]

% Дублирование задач: решение
\label{<problem metapath>:solution}%
\emph{Ответ:} <ответ>.
\ifsolutiondefined
См.~решение задачи \ref{<other problem metapath>:solution}.
\else
\definesolution
<Решение.>
\fi % \ifsolutiondefined

% Дублирование задач: картинка к решению
\begingroup % figure
\ifsolution\def\solutioninclude#1{#1}\else\def\solutioninclude#1{}\fi
\solutioninclude{\ifsolutiondefined\else
\begin{figure}\centering
    \jeolmfigure[width=0.5\textwidth]{solution}
    \caption{к решению задачи \ref{<problem metapath>:solution}}
    \label{<problem metapath>:solution:fig}
\end{figure}%
\fi}%
\endgroup % figure

